security-tools-compose is a simple configuration for docker-compose to facilitate running OWASP WebGoat and OWASP ZAP in docker containers simultaneously.

Steps to use:
- [] Clone this repo:
    - []  